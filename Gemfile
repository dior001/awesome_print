# frozen_string_literal: true

source 'https://rubygems.org'

# Specify the gem's own dependencies in awesome_print.gemspec.
gemspec

# Optional integrations that Awesome Print detects and formats when present.
# They are exercised by the test suite but are NOT runtime dependencies of the
# gem itself, so they live here rather than in the gemspec.
group :test do
  gem 'mongoid', '>= 7.0'
  gem 'rails', '>= 6.1'
end
